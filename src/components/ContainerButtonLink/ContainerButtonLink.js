import "./styles.css";

const ContainerButtonLink = ({children}) => {
    return (
        <div className="containerSection">
            {children}
        </div>
    );
}

export {ContainerButtonLink};